import styled from "styled-components"

export const Input = styled.input`
  padding: 10px 14px;
  border-radius: 5px;
  outline: none;
  border: 2px solid #646ff0;
  margin-right: 4px;
  font-size: 1.8rem;
  &::placeholder{
    opacity: 0.8;
  }
`
